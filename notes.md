Git :

git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/pgtpg/pgtpg.github.io.git
git push -u origin main

pour la dernière ligne, --force si besoin
cf. https://stackoverflow.com/questions/25862329/git-giving-fetch-first-error-when-trying-to-push

# Mise à jour

Lancer les commandes git suivantes :

git add .
git commit -m "first commit"
git push -u origin main
